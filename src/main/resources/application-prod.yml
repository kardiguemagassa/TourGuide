server:
  port: ${SERVER_PORT:8092}  # Utiliser la variable d'environnement

logging:
  level:
    com:
      openclassrooms:
        tourguide: DEBUG
    org:
      springframework: INFO
    root: INFO

management:
  server:
    port: ${SERVER_PORT:8092}  # Même port que l'application
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always