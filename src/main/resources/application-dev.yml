server:
  port: ${SERVER_PORT:8090}

logging:
  level:
    com:
      openclassrooms:
        tourguide: DEBUG
    org:
      springframework: DEBUG
    root: INFO

management:
  server:
    port: ${SERVER_PORT:8090}
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env
  endpoint:
    health:
      show-details: always